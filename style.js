alert('connected')

let firstName = 'Red'
let lastName =  'Sox'
let city = 'Boston'
let wins = '250'
const game = 'Baseball'

let fullName = firstName + ' ' + lastName

let team = ` The ${fullName}, from ${city} have over ${wins} wins in the ${game} world wide.`

Console.log (team)
